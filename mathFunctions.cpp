#include <iostream>
#include <cmath>

int main(){

    double x;
    double y;
    double z;
    double test;
    x = 4;
    y = 5;
    // z = std::max(x,y);
    // z = std::min(x,y);
    // z = pow(x,y);
    // z = sqrt(x);
    // z = abs(-7);


    // test = 3.99;
    // z = round(test); rounds 'x' to the nearest whole number

    // test = 3.1;
    // z = ceil(x); rounds 'x' up to the next whole number regardless of value

    // test = 3.99;
    // z = floor(test);


    std::cout << z;

    return 0;
}